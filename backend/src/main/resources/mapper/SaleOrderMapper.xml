<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.carsale.mapper.SaleOrderMapper">

    <select id="createSaleOrder" statementType="CALLABLE">
        {call process_sale_order(
                #{customerId, mode=IN, jdbcType=INTEGER},
                #{operatorId, mode=IN, jdbcType=INTEGER},
                #{paymentMethod, mode=IN, jdbcType=VARCHAR},
                #{saleItemsJson, mode=IN, jdbcType=OTHER}
              )}
    </select>

</mapper>