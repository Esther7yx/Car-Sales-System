<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.carsale.mapper.CustomerMapper">

    <select id="selectAll" resultType="com.carsale.entity.Customer">
        SELECT * FROM customer
        <where>
            <if test="keyword != null and keyword != ''">
                name LIKE CONCAT('%', #{keyword}, '%')
                OR phone LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

    <select id="selectById" resultType="com.carsale.entity.Customer">
        SELECT * FROM customer WHERE customer_id = #{customerId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="customerId">
        INSERT INTO customer (customer_type, name, id_number, phone, email, address, credit_rating)
        VALUES (#{customerType}, #{name}, #{idNumber}, #{phone}, #{email}, #{address}, #{creditRating})
    </insert>

    <update id="update">
        UPDATE customer SET
                            customer_type = #{customerType},
                            name = #{name},
                            id_number = #{idNumber},
                            phone = #{phone},
                            email = #{email},
                            address = #{address},
                            credit_rating = #{creditRating}
        WHERE customer_id = #{customerId}
    </update>

    <delete id="deleteById">
        DELETE FROM customer WHERE customer_id = #{customerId}
    </delete>
</mapper>